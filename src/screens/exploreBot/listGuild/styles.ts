import { ListItemButton, Box } from '@mui/material'
import { styled } from '@mui/material/styles'

export const Wrapper = styled(Box)`
    padding: 2rem 2rem; */
`

export const LiistItemButtonCustom = styled(ListItemButton)``
